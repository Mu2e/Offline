#include "JobConfig/TDR/geom_baseline.txt"

vector<string> g4SteppingAction.killInTheseVolumes = { "DS2Vacuum", "DS3Vacuum", "HallAir" };

double g4.minRangeCut     = 0.100; // mm

// These are used in both SteppingAction and StackingAction.
bool   g4.killLowEKine  = true;
double g4.eKineMin      = -1.0;    // Kinetic energy in MeV: negative to not kill
vector<int>    g4.killLowEKinePDG = { 11, -11, 22, 2112 }; // 
vector<double> g4.eKineMinPDG     = { 50, 50, 50, 50 }; // MeV

// List of PDG ids of particles for which to switch off decay.
// Keep both pi+ and pi-: pi+ is interesting for calibration
vector<int> g4.noDecay = {211, -211};

// This tells emacs to view this file in c++ mode.
// Local Variables:
// mode:c++
// End:
