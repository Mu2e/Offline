#include "minimalMessageService.fcl"
#include "standardProducers.fcl"

process_name: BFieldTest

source: {
    module_type: EmptyEvent
    maxEvents: 1
}

services: {
    message: @local::default_message

//    // This does not help to get clean outputs because of BaBar singleton material printouts and other stuff
//    message : {
//        destinations :
//        {
//            log:      @local::mf_null
//            statistics :   { stats: @local::mf_null }
//        }
//    }

    scheduler: { defaultExceptions : false }

    user: {
        GeometryService   : { inputFile : "Mu2eG4/test/geom_01.txt" }
        ConditionsService : { conditionsfile : "Mu2eG4/test/conditions_01.txt" }
    }
}

physics: {
    analyzers: {
        bftest: {
            module_type: BFieldTest
            x : 3904
            y : 0

            zmin :      0
            zmax : -18000
            npoints : 180

            // // Points near the PS map boundary: one inside, one outside of the "inner" map
            // zmin :  -9929.01
            // zmax :  -9928.99
            // npoints :   2
        }
    }

    e1: [bftest]
    end_paths: [e1]
}
