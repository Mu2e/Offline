#include "Mu2eG4/geom/geom_common.txt"

// replace the CRV counter setup:

int   crs.nSectors = 1;
int   crs.nLayers  = 4;

vector<string>  crs.sectorNames = {"Test"};

double          crs.scintillatorBarLengthTest    =  5600;

double          crs.scintillatorBarThickness     =  20.0; //mm   //is actually only 19.8mm, while the lookup tables have 20.0mm
double          crs.scintillatorBarWidth         =  50.0; //mm   //current Offline has 49.5mm, while the lookup tables have 50.0mm
double          crs.layerOffset                  =  10;   //mm
double          crs.gapLarge                     =   2;   //mm
double          crs.gapSmall                     =   1;   //mm
double          crs.gapBetweenModules            =   5;   //mm
vector<double>  crs.gapBetweenLayers             =   {9.525, 12.7, 9.525}; //mm

int             crs.nModulesTest                 =   6;
int             crs.nCountersPerModuleTest       =   16;
vector<double>  crs.firstCounterTest             =  {-3904.0, 2600.6, 0}; // next modules start at z=827, 1654, ..., 4135

string          crs.scintillatorBarMaterialName  = "G4_POLYSTYRENE";
string          crs.absorberMaterialName         = "G4_Al";

bool   crs.vetoVisible    = true;
bool   crs.vetoSolid      = true;
int    crs.verbosityLevel = 0;

vector<double>  crs.layerDirectionTest           =  {0, 1, 0};
vector<double>  crs.offsetDirectionTest          =  {0, 0, -1}; 
vector<double>  crs.gapDirectionTest             =  {0, 0, 1};


// This tells emacs to view this file in c++ mode.
// Local Variables:
// mode:c++
// End:
