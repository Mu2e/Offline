#include "Mu2eG4/geom/geom_common.txt"

bool hasSTM = false;

// replace the CRV counter setup:

int   crs.nSectors = 1;
int   crs.nLayers  = 4;

vector<string>  crs.sectorNames = {"Test"};

double          crs.scintillatorBarLengthTest    =  6600;

double          crs.scintillatorBarThickness     =  20.0; //mm   //is actually only 19.8mm, while the lookup tables have 20.0mm
double          crs.scintillatorBarWidth         =  50.0; //mm   //current Offline has 49.5mm, while the lookup tables have 50.0mm
double          crs.layerOffset                  =  42.0; //mm
double          crs.gapLarge                     =   1.0; //mm
double          crs.gapSmall                     =   0.5; //mm
double          crs.gapBetweenModules            =   5.0; //mm
vector<double>  crs.gapBetweenLayers             =   {9.525, 9.525, 9.525}; //mm

int             crs.nModulesTest                 =   4;
int             crs.nCountersPerModuleTest       =   16;
vector<double>  crs.firstCounterTest             =  {-3404.0, 2600.6, 0}; // next modules start at z=814, 1628, 2442

vector<double>  crs.layerDirectionTest           =  {0, 1, 0};
vector<double>  crs.offsetDirectionTest          =  {0, 0, -1}; 
vector<double>  crs.gapDirectionTest             =  {0, 0, 1};

int crs.precedingSectorForTest  = -1;   //no preceding sector

int crs.sectorTypeTest  = 1;

bool crs.sipmsAtSide0Test = true;
bool crs.sipmsAtSide1Test = true;

int crs.FEBBoxesAtSide0Test  = 0;
int crs.FEBBoxesAtSide1Test  = 0;

// This tells emacs to view this file in c++ mode.
// Local Variables:
// mode:c++
// End:
