# Example of exploring the Kalman filter output.
# Needs to stay in sync with TrkPatRec/test/TrkPatRecTest02.fcl
#
# $Id: readKalFits.fcl,v 1.2 2012/07/20 23:04:46 kutschke Exp $
# $Author: kutschke $
# $ $

#include "minimalMessageService.fcl"
#include "standardProducers.fcl"
#include "standardServices.fcl"

process_name : ReadKalFits

source : {
  module_type : EmptyEvent
  maxEvents   : 100
}

services : {
  message               : @local::default_message
  TFileService          : { fileName : "readKalFits.root" }
  RandomNumberGenerator : { }

  user : {
    GeometryService        : { inputFile : "Mu2eG4/test/geom_01.txt" }
    ConditionsService      : { conditionsfile : "Mu2eG4/test/conditions_01.txt" }
    GlobalConstantsService : { inputFile : "Mu2eG4/test/globalConstants_01.txt" }
    BTrkHelper             : @local::BTrkHelperDefault
    G4Helper               : { }
    SeedService            : @local::automaticSeeds
  }

}

physics : {

  producers : {
    generate: @local::generate
    g4run : @local::g4run
    makeSH : @local::makeSH
    TrkPatRec : @local::TrkPatRec
  }

  analyzers : {
    readfits : {
      module_type       : ReadKalFits
      fitterModuleLabel : TrkPatRec
      WeightEvents      : false
      KalFitMC          : { }
      verbosity         : 2
      maxPrint          : 200
   }
  }


  p1 : [generate , g4run, makeSH, TrkPatRec ]
  e1 : [ readfits ]

  trigger_paths  : [p1]
  end_paths      : [e1]

}

physics.producers.generate.inputfile       : "Mu2eG4/test/genconfig_01.txt"
physics.producers.makeSH.diagLevel         :   0
physics.producers.TrkPatRec.diagLevel      :   2
physics.producers.makeSH.maxFullPrint      :   0
services.user.SeedService.baseSeed         :   0
services.user.SeedService.maxUniqueEngines :  20
